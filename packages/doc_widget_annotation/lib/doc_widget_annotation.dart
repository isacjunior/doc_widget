library doc_widget_annotation;

/// Annotation for the `doc_widget` package.
class DocWidget {
  const DocWidget._();
}

/// Annotation for the `doc_widget` package.
///
/// This annotation is used to generate your documentation
///
/// ```dart
/// @docWidget
/// class MyWidget extends StatelessWidget {
/// }
/// ```
const docWidget = DocWidget._();
